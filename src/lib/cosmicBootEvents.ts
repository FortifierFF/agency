/** Dispatched from the WebGL loop when the *boot* intro hyperspace envelope has decayed (camera ~still). */
export const APEX_INTRO_HYPERSPACE_SETTLED = "apex-intro-hyperspace-settled" as const;

/** Dispatched when an in-app route-change hyperspace pulse has decayed (camera ~still again). */
export const APEX_ROUTE_HYPERSPACE_SETTLED = "apex-route-hyperspace-settled" as const;
